<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.dvarahq.oss</groupId>
    <artifactId>evals4j-parent</artifactId>
    <version>0.5.0-SNAPSHOT</version>
  </parent>

  <artifactId>evals4j-spring-boot-starter</artifactId>
  <name>evals4j :: spring-boot-starter</name>
  <description>Auto-configuration binding evals4j to whichever chat model is on the classpath</description>

  <properties>
    <automatic.module.name>com.dvarahq.oss.evals4j.autoconfigure</automatic.module.name>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.dvarahq.oss</groupId>
      <artifactId>evals4j-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
    </dependency>

    <dependency>
      <groupId>com.dvarahq.oss</groupId>
      <artifactId>evals4j-springai</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.dvarahq.oss</groupId>
      <artifactId>evals4j-langchain4j</artifactId>
      <optional>true</optional>
    </dependency>
    <!-- Backs MicrometerEvalTracer. micrometer-core carries MeterRegistry; micrometer-observation
         alone does not, and recording a score is a metric rather than an observation. -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-core</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-configuration-processor</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
